# wrangler.toml — hanya untuk local dev (npx wrangler pages dev .)
# JANGAN dipakai sebagai build/deploy command di CF Pages dashboard
name = "web-tools-dhafa"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "."
